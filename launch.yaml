# This is the base file for Coordination Application
# world config: ADF/world/world.yaml
world config: ADF/world/world.yaml
color config: ADF/color/colors.yaml
input devices config: ADF/input_devices/input_devices.yaml
plugins: [
  {
    name: PBR_PLUGIN,
    filename: libpbr_plugin.so
  }
]
multibody configs:
  - "ADF/sphere.yaml" #0
